import React from "react";

const Footer = () => {
  return (
    <div
      className=" text-center"
      style={{ paddingTop: "95px", position: "sticky" }}
    >
      <p className="text-muted small opacity-0 ">
        *Powered by Claud(AWS).Vertex(Google).OpenAI(Microsoft)
      </p>
    </div>
  );
};

export default Footer;
